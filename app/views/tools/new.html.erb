<h1 id='tool_header'>New Tool</h1>
<br/>
<%= render :partial => 'form' %>

<a href='/tools' class='btn'>Cancel</a>

<script type='text/javascript'>
	<% if @corpus %>
		add_to_corpora('<%= @corpus.name %>', '<%= @corpus.id %>');
	<% end %>
	$('#tool_name').focus();
</script>
<%= render :partial => 'shared/topnav_select_active', :locals => {:select => 'tools'} %>