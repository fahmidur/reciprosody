<div class='row-fluid'>
	<div class='span12'>

	<%= render :partial => 'shared/btns/back' %>

	<%= render :partial => 'shared/btns/list', :locals => {
		:href => tools_path,
		:text => 'All Tools'
	}%>

	<% if @tool.canEdit?(current_user) %> 
		<%= render :partial => 'shared/btns/edit', :locals => {
			:href => edit_tool_path(@tool)
		}%>

		<a href="#delete-confirm-tool-<%=@tool.id%>" data-id="<%= @tool.id %>" class="delete-confirm btn btn-danger pull-right" data-toggle="modal">
			<i class='icon-trash'></i>&nbsp;
			Delete
		</a>

		<div class="modal fade" id="delete-confirm-tool-<%= @tool.id %>" style="display:none">
			<div class="modal-header">
				<a class="close" data-dismiss="modal">×</a>
				<h3>Are you sure?</h3>
			</div>
			<div class="modal-body">
				<p>Just checking.</p>
			</div>
			<div class="modal-footer">
				<%= link_to 'Destroy', '/tools/'+ @tool.id.to_s, :method => :delete, 
					:class => 'btn' %>
				<a href="#" data-dismiss="modal" class="btn">Cancel</a>
			</div>
			</div>

	<% end %>
	</div>
</div>