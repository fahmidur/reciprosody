# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html

# I own Corpus One
corpus_one_membership_owner:
  user_id: <%= User.find_by_name("Syed Reza").id %>
  corpus_id: 1
  role: owner

# I own Corpus Two
corpus_two_membership_owner:
  user_id: <%= User.find_by_name("Syed Reza").id %>
  corpus_id: 2
  role: owner

# One Doe owns Corpus Three
corpus_three_membership_owner:
  user_id: <%= User.find_by_name("One Doe").id %>
  corpus_id: 3
  role: owner
